# Shared GitHub community health files

This repository provides default community health files for repositories owned by `gaston-ara`. GitHub applies these files when a repository does not define its own equivalent file.

Feature requests must use the shared issue form. New requests receive `status:needs-review` and `type:feature`; implementation begins only after a maintainer reviews the request, adds `status:approved`, and removes `status:needs-review`.
